# dbt_project.yml

name: 'mozart_api_project'
version: '1.0'
config-version: 2

profile: 'mozart'   # 👈 MUST be exactly 'mozart'

model-paths: ["models"]

target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  mozart_api_project:
    +materialized: view
